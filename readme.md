# Pratices  DOCKER 

```
Build the image : docker compose build

Run the containers : docker compose up



sudo docker inspect tp_db_1 

info : "IPAddress": "172.28.0.2"

sudo docker inspect tp_gestion_1

info: "IPAddress": "172.28.0.3"

sudo docker inspect tp_nginx_1

info : "IPAddress": "172.28.0.4"

Close the containers : docker compose down
```
